---
scope: Financas — single-user browser-based investment-focused personal finance manager
purpose: build-substrate
altitude: feature
updated: 2026-06-28T19:28
---

- (version) Verified June 2026: Go 1.26.4 (latest stable), PostgreSQL 18.4 (latest stable; 19 in beta, not used).
- (version) Web stack (seed, pinned at scaffold): chi v5, pgx v5, sqlc, goose, templ, HTMX 2.x, Tailwind 4.x, shopspring/decimal. Going greenfield Go web path verified on web.
- (direction) Paradigm = layered onion: http->service->store over dependency-free domain/money core. Recommended greenfield path for solo builder + AI dev agents.
- (decision) AD-1 layered dependency direction (inward only). AD-2 transaction ledger = single source of truth; holdings/balances/valuations derived [ADOPTED from PRD]. AD-3 single mutation path, one DB tx per use-case.
- (decision) AD-4 decimal money/quantity never float (NUMERIC + shopspring/decimal) [ADOPTED]. AD-5 store native currency, convert only at read time via effective-dated rate. AD-6 prices+FX owner-entered effective-dated time series, no external feed [ADOPTED].
- (decision) AD-7 single owner, authenticated everywhere, no tenant column (single-tenancy is invariant). AD-8 single Docker image -> Azure Container Apps + Azure DB for PostgreSQL Flexible Server; config via env; local dev via Docker Compose.
- (assumption) ASSUMPTIONS: Container Apps over App Service; cookie server session; bigint identity PKs over UUID; NUMERIC(19,4) money / NUMERIC(28,10) qty scales; Chart.js-class charting lib for dashboard.
- (event) Lint clean (0 findings). Adversarial reviewer: PASS-WITH-FIXES; review-adversarial.md.
- (decision) AD-9 (Transfer = single row, two-account, derived rate), AD-10 (one canonical domain home per derived figure; http no math), AD-11 (value-over-time derived from effective-dated history, no snapshot table), AD-12 (banker's rounding, convert-then-sum, allocation reconciled 100%, cumulative realized gain at each Sell's effective rate).
- (decision) Tightened AD-2 (realized gain derived not stored; shared basis_sold fn), AD-5 (conversion->AD-12), AD-6 (rates directional, no inversion), AD-7 (argon2id). Conventions added: avg-cost formula+banker's rounding, rate scale NUMERIC(18,8), import dedup key+hash, year pivot, category-type rule, archived excluded from both totals, export authored-only + identity-insert restore, amounts magnitude+type.
- (version) Reviewer claimed PG 18.4 implausible, but web search confirmed postgresql.org 18.4 released 2026-05-11 — kept. Fixed http.CrossOriginProtection attribution to Go 1.25+ (not 1.26).
- (event) spine finalized
